cmake_minimum_required(VERSION 3.3)

set(SRCS main.cpp)

add_executable(network ${SRCS})

set_property(TARGET network PROPERTY FOLDER "executables")

install (TARGETS network RUNTIME DESTINATION ${PROJECT_BINARY_DIR}/bin)
